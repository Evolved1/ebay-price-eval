import eBay_scrape
import error_function
import target_item

def main():
    target_item.single_or_mass()


    #target_item.url(new_search)
    soup = get_data(url)
    products = parse(soup)
    export(products, search_term)


main()